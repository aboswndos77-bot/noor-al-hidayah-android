import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.lovable.p8acbe9835ef144c1b3b3c9169360a2fb",
  appName: "نور الهداية",
  webDir: "dist",
  android: {
    allowMixedContent: true,
  },
};

export default config;
